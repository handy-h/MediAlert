# CR 问题全部修复 — 完成总结

**时间**: 2026-06-10
**项目**: MediAlert（药箱库存管家）— `D:\Builds\MediAlert`

## 修复覆盖

### P0 (Critical) — 国际化
| 问题 | 修复 |
|------|------|
| 无英文回退目录 | ✅ 创建 `values-en/strings.xml`（104 行英文资源） |
| 代码中 30+ 硬编码中文 | ✅ 全部迁移到 `values/strings.xml` 新增资源（70+ 新增条目） |
| MedicationCard 硬编码 UI | ✅ 改用 `stringResource(R.string.xxx, args...)` |
| CalendarManager 硬编码日历事件 | ✅ 改用 `context.getString(R.string.xxx, args...)` |
| MedicationViewModel CSV 字符串 | ✅ 导出/导入均使用 `context.getString()` |
| MergedAlertScreen 硬编码 | ✅ `stringResource()` + `Locale.getDefault()` |
| AddMedicationScreen 下拉列表 | ✅ `resources.getStringArray()` 动态加载 |
| StockInputDialog 标签 | ✅ `stringResource()` |
| AlarmReceiver 硬编码回退名 | ✅ `context.getString(R.string.medication_fallback)` |
| EditMedicationScreen "天" | ✅ `stringResource(R.string.day_unit)` |

### P1 (High) — 通知/权限
| 问题 | 修复 |
|------|------|
| 通知渠道未预创建 | ✅ `MediAlertApplication.onCreate()` 中创建 |
| AlarmReceiver 重复创建渠道 | ✅ 改为先检查后创建的安全回退模式 |
| 无电池优化/自启动引导 | ✅ SettingsScreen 新增电池优化入口 → 跳转系统设置 |
| 日历权限失败静默 | ✅ SettingsScreen 权限拒绝后 Toast 提示「仅通知闹钟」 |
| 通知未分组 | ✅ `setGroup("medialert_alerts")` |

### P2 (Medium) — UI/代码质量
| 问题 | 修复 |
|------|------|
| medicationCard 按钮可能溢出 | ✅ 图标缩小到 18dp、间距缩小到 2dp、文字 `maxLines=1` + `TextOverflow.Ellipsis` |
| 精确闹钟权限仅启动时检查 | ✅ SettingsScreen 新增精确闹钟状态入口 |
| CSV 导入仅支持中文频率格式 | ✅ 新增英文正则 `Every X days` / `Every Xth day` 解析 |

### Bug Fix
| 问题 | 修复 |
|------|------|
| `resetAllReminders` 返回 activeMeds 总数而非实际注册数 | ✅ 改为 `activeMeds.count { it.daysUntilDepletion() > 0 }` |

### 图标
| 问题 | 修复 |
|------|------|
| App 图标使用 adaptive vector icon | ✅ 删除 `mipmap-anydpi-v26/ic_launcher*.xml`，回退到 PNG 图标 |

### Code Review 后续修复
| 问题 | 修复 |
|------|------|
| `Icons.Filled.ArrowBack` 废弃警告 | ✅ 5 个文件全部替换为 `Icons.AutoMirrored.Filled.ArrowBack` |
| `Divider()` 废弃警告 | ✅ 5 处全部替换为 `HorizontalDivider()` |
| `calendarPermissionGranted` 未使用变量 | ✅ 删除 |
| 按钮文字截断无提示 | ✅ 添加 `overflow = TextOverflow.Ellipsis` |

## 修改文件清单

| 文件 | 操作 |
|------|------|
| `res/values-en/strings.xml` | 新建（104 行英文资源） |
| `res/values/strings.xml` | 追加（70+ 新资源定义） |
| `calendar/CalendarManager.kt` | 修改（4 处 i18n） |
| `ui/components/MedicationCard.kt` | 修改（5 处 i18n + 按钮溢出修复 + Ellipsis） |
| `ui/components/StockInputDialog.kt` | 修改（2 处 i18n） |
| `ui/screens/AddMedicationScreen.kt` | 修改（下拉列表 + ArrowBack 修复） |
| `ui/screens/EditMedicationScreen.kt` | 修改（1 处 i18n + ArrowBack 修复） |
| `ui/screens/MergedAlertScreen.kt` | 修改（4 处 i18n + Locale + ArrowBack 修复） |
| `ui/screens/SettingsScreen.kt` | 重写（电池优化 + 精确闹钟 + 日历回退 + i18n + HorizontalDivider） |
| `ui/screens/InactiveMedicationsScreen.kt` | 修改（ArrowBack 修复） |
| `alarm/AlarmReceiver.kt` | 修改（统一 CHANNEL_ID + 分组 + 回退名） |
| `viewmodel/MedicationViewModel.kt` | 修改（CSV i18n + 双语言正则 + reset 计数修复） |
| `MediAlertApplication.kt` | 修改（预创建通知渠道） |
| `mipmap-anydpi-v26/` | 删除（adaptive icon XML files） |

## 构建状态
✅ `BUILD SUCCESSFUL` — 无任何编译错误/警告（废弃 API 警告已全部修复）。
